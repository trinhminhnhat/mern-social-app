import { Box } from '@mui/material';
import { Navbar } from 'components';

const Homepage = () => {
    return (
        <Box>
            <Navbar />
        </Box>
    );
};

export default Homepage;
